// With redux we need to define something called action types.
// we actually define them as constants in a file.

// This will be a central place for all of our types
export const GET_CONTACTS = "GET_CONTACTS";
